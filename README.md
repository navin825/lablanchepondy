# La Blanche — Vite React site
Simple Vite React project ready to deploy on Vercel / Netlify.

## How to test locally
1. `npm install`
2. `npm run dev`
3. Open http://localhost:5173

## Deploy to Vercel
- Push this repo to GitHub, then import into Vercel, or drag & drop the folder in Vercel's dashboard.